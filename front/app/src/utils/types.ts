export type Property = {
    id: string
    name: string
    updatedAt: string
    fromStation: string
    address: string
    lat: number
    lng: number
    rent: number
};
export type Advice = {
  id: string;
  text: string;
  iconSrc: string;
};
