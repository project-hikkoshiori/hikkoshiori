export type Property = {
    id: string
    name: string
    updatedAt: string
    fromStation: string
    address: string
    rent: number
};
export type Advice = {
  id: string;
  text: string;
  iconSrc: string;
};
