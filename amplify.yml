version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd front/app
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: front/app/.next
    files:
      - "**/*"
  cache:
    paths:
      - node_modules/**/*